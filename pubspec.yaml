name: github_api
description: A commandline client and Dart library for access on the GitHub API.
# version: 1.0.0
# homepage: https://www.example.com

executables:
  github: main

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  http: ^0.12.2

dev_dependencies:
  test: ^1.14.4
